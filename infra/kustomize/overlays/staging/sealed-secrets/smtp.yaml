# Placeholder sealed secret for SMTP credentials in staging.
# Use kubeseal to regenerate encrypted values before deployment.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: village-storefront-mailer
  namespace: village-storefront-staging
spec:
  encryptedData:
    username: PLACEHOLDER
    password: PLACEHOLDER
  template:
    metadata:
      name: village-storefront-mailer
      namespace: village-storefront-staging
    type: Opaque
