# Placeholder sealed secret for Stripe API keys in staging.
# Replace `encryptedData` values with output from `kubeseal` scoped to the staging cluster before applying.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: village-storefront-stripe
  namespace: village-storefront-staging
spec:
  encryptedData:
    api-key: PLACEHOLDER
    webhook-secret: PLACEHOLDER
  template:
    metadata:
      name: village-storefront-stripe
      namespace: village-storefront-staging
    type: Opaque
