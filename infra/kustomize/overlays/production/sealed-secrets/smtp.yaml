# Placeholder production sealed secret for SMTP credentials.
# Always regenerate encryptedData per cluster to avoid failed decryptions.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: village-storefront-mailer
  namespace: village-storefront
spec:
  encryptedData:
    username: PLACEHOLDER
    password: PLACEHOLDER
  template:
    metadata:
      name: village-storefront-mailer
      namespace: village-storefront
    type: Opaque
