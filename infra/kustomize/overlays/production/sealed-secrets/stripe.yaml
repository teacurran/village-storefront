# Placeholder production sealed secret for Stripe API keys.
# Replace encryptedData with kubeseal output targeting the production controller before applying.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: village-storefront-stripe
  namespace: village-storefront
spec:
  encryptedData:
    api-key: PLACEHOLDER
    webhook-secret: PLACEHOLDER
  template:
    metadata:
      name: village-storefront-stripe
      namespace: village-storefront
    type: Opaque
