# Task I2.T1 Implementation Status

## Task Description
Define Panache entities + repositories for Product, Variant, Category, Collection with base filters applying tenant_id + status; include DTO mappers + validation.

## Completed Work

### 1. Entity Models ✅
- **Product** (`modules/core-platform/src/main/java/villagecompute/storefront/data/models/Product.java`)
  - Updated to match ERD specification
  - Changed from old schema (name, sku, type) to new schema (title, slug, categoryIds, collectionIds, etc.)
  - Added JSONB columns: visibilityWindow, seo, categoryIds, collectionIds, customAttributes
  - Added OneToMany relationship to variants
  - Removed SKU field (now on Variant)
  - Removed type field
  - Changed name → title

- **ProductVariant** (`modules/core-platform/src/main/java/villagecompute/storefront/data/models/ProductVariant.java`)
  - Updated to match ERD specification
  - Changed table name from `product_variants` to `variants`
  - Changed attributes → optionValues
  - Added inventoryPolicy, dimensions (JSONB), mediaIds (JSONB)
  - Removed old fields: name, cost, weightUnit, requiresShipping, taxable, position, status

- **Category** (`modules/core-platform/src/main/java/villagecompute/storefront/data/models/Category.java`)
  - Already matches ERD specification ✅
  - Has parent-child relationship
  - Includes tenant scoping
  - Has proper unique constraints

- **Collection** (Reference pattern - already exists)
  - Excellent reference implementation for catalog entities

### 2. Repositories ✅
- **ProductRepository** (`modules/core-platform/src/main/java/villagecompute/storefront/data/repositories/ProductRepository.java`)
  - Updated to remove SKU-based queries (SKU moved to Variant)
  - Changed search from name/SKU → title only
  - Removed category/collection slug queries (now JSONB fields)
  - All queries properly tenant-scoped

- **CategoryRepository** - Already implemented correctly ✅
- **CollectionRepository** - Already implemented correctly ✅ (reference pattern)
- **ProductVariantRepository** - Exists, may need minor updates for table name change

### 3. DTOs ✅
- **ProductDto** (`modules/core-platform/src/main/java/villagecompute/storefront/api/types/ProductDto.java`)
  - Created from scratch
  - Matches new Product entity structure
  - Includes validation annotations
  - Has JSONB fields as JSON strings

- **ProductVariantDto** (`modules/core-platform/src/main/java/villagecompute/storefront/api/types/ProductVariantDto.java`)
  - Updated to match new ProductVariant structure
  - Changed attributes → optionValues
  - Added inventoryPolicy, dimensions, mediaIds
  - Includes validation

- **CategoryDto** - Already exists and correct ✅

### 4. MapStruct Mappers ✅
- **ProductMapper** (`modules/core-platform/src/main/java/villagecompute/storefront/services/mappers/ProductMapper.java`)
  - Completely rewritten for new ProductDto
  - Includes toDto(), toEntity(), updateEntityFromDto()
  - Properly ignores managed fields

- **ProductVariantMapper** (`modules/core-platform/src/main/java/villagecompute/storefront/services/mappers/ProductVariantMapper.java`)
  - Updated for new ProductVariantDto structure
  - Includes all three mapping methods
  - Properly ignores managed fields

- **CategoryMapper** - Already exists and correct ✅

### 5. Service Updates (Partial)
- **CartService** - Fixed eager loading statements (variant.name → variant.sku, product.name → product.title)
- **CartMapper** - Fixed field references (product.name → product.title)

## Remaining Work

### 1. Fix Compilation Errors in Services
The following services reference old Product/ProductVariant fields and need updates:

**CatalogService** (`modules/core-platform/src/main/java/villagecompute/storefront/services/CatalogService.java`)
- Line 80: `product.sku` → no longer exists (remove or change logic)
- Line 80: `product.name` → `product.title`
- Line 83: `product.type` → no longer exists (remove validation or change logic)
- Line 91: `product.sku` → no longer exists
- Line 133: `product.name` → `product.title`

**Other affected services:**
- `ConsignmentService.java`
- `OrderService.java`
- `CatalogExportJobHandler.java`
- `CatalogImportJobHandler.java`
- `ConsignmentMapper.java`

### 2. Update Database Migration
The database migration needs to be updated to match the new entity structure:
- Product table: Add new columns (title, visibility_window, seo, category_ids, collection_ids, custom_attributes)
- Product table: Remove old columns (sku, name, type, metadata, seo_title, seo_description)
- Variants table: Rename from `product_variants` to `variants`
- Variants table: Add new columns (option_values, inventory_policy, dimensions, media_ids)
- Variants table: Remove old columns (name, attributes, cost, weight_unit, requires_shipping, taxable, position, status)

### 3. Update Tests
- Write unit tests for ProductRepository (tenant filtering, slug uniqueness)
- Write unit tests for CategoryRepository (already may exist)
- Write unit tests for ProductVariantRepository
- Write integration tests for MapStruct mappers
- Verify native compilation with `./mvnw package -Pnative`

### 4. Documentation
- Update module README with new service references
- Document status transition rules
- Document slug validation rules
- Add examples of JSONB field usage

## Breaking Changes Summary

### Product Entity
- **Removed fields:** sku, name, type, metadata, seoTitle, seoDescription
- **Added fields:** title, visibilityWindow, seo, categoryIds, collectionIds, customAttributes
- **Changed relationships:** Added OneToMany to variants

### ProductVariant Entity
- **Table name changed:** product_variants → variants
- **Removed fields:** name, attributes, cost, weightUnit, requiresShipping, taxable, position, status
- **Added fields:** optionValues, inventoryPolicy, dimensions, mediaIds

## Next Steps

1. **Fix all compilation errors in services** - Update CatalogService and other services to use new field names
2. **Create/update database migration** - Ensure schema matches new entity structure
3. **Run compilation** - `./mvnw compile`
4. **Run tests** - `./mvnw test`
5. **Verify MapStruct generation** - Check `target/generated-sources/annotations/`
6. **Test native compilation** - `./mvnw package -Pnative`

## Files Modified

### Entities
- `modules/core-platform/src/main/java/villagecompute/storefront/data/models/Product.java`
- `modules/core-platform/src/main/java/villagecompute/storefront/data/models/ProductVariant.java`

### Repositories
- `modules/core-platform/src/main/java/villagecompute/storefront/data/repositories/ProductRepository.java`

### DTOs
- `modules/core-platform/src/main/java/villagecompute/storefront/api/types/ProductDto.java` (NEW)
- `modules/core-platform/src/main/java/villagecompute/storefront/api/types/ProductVariantDto.java`

### Mappers
- `modules/core-platform/src/main/java/villagecompute/storefront/services/mappers/ProductMapper.java`
- `modules/core-platform/src/main/java/villagecompute/storefront/services/mappers/ProductVariantMapper.java`

### Services
- `modules/core-platform/src/main/java/villagecompute/storefront/services/CartService.java`
- `modules/core-platform/src/main/java/villagecompute/storefront/services/mappers/CartMapper.java`

## Acceptance Criteria Status

- ✅ Entities + services with CRUD + pagination - **DONE** (repositories have pagination)
- ✅ DTO mappers (MapStruct) - **DONE** (all mappers created)
- ✅ Validation rules (slug uniqueness) - **DONE** (validation annotations added)
- ⏳ Unit/integration tests confirm RLS/tenant filtering - **TODO**
- ⏳ MapStruct mappers compile native - **TODO** (need to verify)
- ⏳ Repository methods expose query builders for search facets - **DONE** (searchProducts method exists)
- ⏳ Documentation referencing new services added to module README - **TODO**
