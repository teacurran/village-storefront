compliance.delete.retention_days=0

# Tenant safety features for tests
%test.tenant.rls.enabled=true
%test.tenant.cache.enabled=true

# Disable Quinoa during tests to avoid build-time config issues
%test.quarkus.quinoa.enabled=false
%test.quarkus.quinoa.enable-spa-routing=false
%test.quarkus.quinoa.build-dir=
